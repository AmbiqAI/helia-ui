---
// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026, Ambiq
/**
 * The uppercase label that sits above a heading.
 *
 * The recipe itself is `.helia-eyebrow` in `recipes.css`.
 *
 * @slot default -- The label text.
 */
import type { HTMLAttributes, HTMLTag } from 'astro/types';

interface Props extends HTMLAttributes<'p'> {
  /** Element to render. Use a `<span>` where a paragraph would break the outline. */
  as?: HTMLTag;
}

const { as: Element = 'p', class: className, ...rest } = Astro.props;
---

<Element class:list={['helia-eyebrow', className]} {...rest}>
  <slot />
</Element>
