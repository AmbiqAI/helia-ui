---
title: Astro part contracts
description: Props, defaults, and slots for every part exported under @ambiqai/helia-ui/astro.
---

{/* Generated by scripts/astro-props.mjs. Edit the parts, not this file. */}

Every part exported under `@ambiqai/helia-ui/astro/*`, read out of the parts
themselves: the `Props` interface, the defaults in the destructure, and the
slots each one documents. A part whose contract changes without this page
changing fails `validate`, so what is here is what the package ships.

Props marked `(req.)` have no default and must be passed. A part that also
lists inherited attributes forwards them to its root element.

## AccordionGroup

**Use it for:** A stack of disclosure rows: one `<details>` per item, the first one open.

```ts
import AccordionGroup from '@ambiqai/helia-ui/astro/AccordionGroup';
```

| Prop           | Type                       | Default | Description                                             |
| -------------- | -------------------------- | ------- | ------------------------------------------------------- |
| `items` (req.) | `readonly AccordionItem[]` | --      | The rows, in display order. The first one renders open. |

**AccordionItem**

| Prop             | Type     | Default | Description                                                |
| ---------------- | -------- | ------- | ---------------------------------------------------------- |
| `title` (req.)   | `string` | --      | The summary line, and the control that opens the row.      |
| `content` (req.) | `string` | --      | The row body. Plain text: it is rendered as one paragraph. |

**Slots** -- none.

[See it rendered on Disclosure](/helia-ui/disclosure/)

## AsciiTerminal

**Use it for:** A terminal transcript: a fixed set of lines, optionally typed out on entry, with optional copy and replay controls.

```ts
import AsciiTerminal from '@ambiqai/helia-ui/astro/AsciiTerminal';
```

| Prop           | Type                              | Default                     | Description                                                              |
| -------------- | --------------------------------- | --------------------------- | ------------------------------------------------------------------------ |
| `lines` (req.) | `readonly AsciiTerminalLine[]`    | --                          | The transcript, in order.                                                |
| `title`        | `string`                          | `'Terminal'`                | The frame's caption.                                                     |
| `ariaLabel`    | `string`                          | `` `${title} transcript` `` | Accessible name for the frame. Defaults to the title plus `transcript`.  |
| `animated`     | `boolean`                         | `false`                     | Types the command lines out on entry. Reduced motion shows them at once. |
| `autoplay`     | `boolean`                         | `true`                      | Starts the animation when the frame scrolls into view. Needs `animated`. |
| `copy`         | `boolean`                         | `false`                     | Adds the copy control, which copies the whole transcript as plain text.  |
| `replay`       | `boolean`                         | `animated`                  | Adds the replay control. Defaults to whatever `animated` is.             |
| `tone`         | `'adaptive' \| 'light' \| 'dark'` | `'adaptive'`                | `adaptive` follows the site theme; `light` and `dark` are fixed.         |
| `typingSpeed`  | `number`                          | `28`                        | Milliseconds per typed character. 0 types a command line instantly.      |
| `lineDelay`    | `number`                          | `130`                       | Milliseconds of pause after each line. 0 runs the lines together.        |

**AsciiTerminalLine**

| Prop          | Type                                                         | Default | Description                                                       |
| ------------- | ------------------------------------------------------------ | ------- | ----------------------------------------------------------------- |
| `text` (req.) | `string`                                                     | --      | The line as it is read and as it is copied, without the prompt.   |
| `kind`        | `'command' \| 'muted' \| 'output' \| 'success' \| 'warning'` | --      | What the line is. Only `command` is typed out and given a prompt. |
| `prompt`      | `string`                                                     | --      | Prompt in front of a `command` line. Ignored on every other kind. |

**Slots** -- none.

[See it rendered on Code](/helia-ui/code/#ascii-terminal)

## Badge

**Use it for:** A read-only label on the `Chip` recipe, tinted by an advisory tone.

```ts
import Badge from '@ambiqai/helia-ui/astro/Badge';
```

| Prop   | Type                                                                    | Default     | Description                                                                            |
| ------ | ----------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------- |
| `tone` | `'neutral' \| 'info' \| 'success' \| 'warning' \| 'danger' \| 'accent'` | `'neutral'` | The advisory tint. `neutral` and `accent` reuse the chip tones; the rest are tinted.   |
| `dot`  | `boolean`                                                               | `false`     | Leading status dot, drawn in the badge ink. Decorative: the label carries the meaning. |
| `size` | `'sm' \| 'md'`                                                          | `'sm'`      | `sm` is the in-content metadata size, `md` the standalone one.                         |

Also accepts `HTMLAttributes<'span'>`.

**Slots**

- `default` -- The label. Required: without it the badge renders nothing.

[See it rendered on Cards](/helia-ui/cards/#badge)

## Button

**Use it for:** The call to action: a pill-shaped link or button.

```ts
import Button from '@ambiqai/helia-ui/astro/Button';
```

| Prop      | Type                                  | Default     | Description                                                           |
| --------- | ------------------------------------- | ----------- | --------------------------------------------------------------------- |
| `variant` | `'primary' \| 'secondary' \| 'ghost'` | `'primary'` | `primary` is the filled ink pill, `secondary` outlined, `ghost` bare. |
| `size`    | `'sm' \| 'md'`                        | `'md'`      | `md` is the hero and page-level size, `sm` the in-content one.        |
| `href`    | `string`                              | --          | Where it goes. Its presence is what makes this an anchor.             |

Also accepts `Omit<HTMLAttributes<'a'>, 'href'>`, `HTMLAttributes<'button'>`.

**Slots**

- `default` -- The label, and the accessible name of the control.

[See it rendered on Primitives](/helia-ui/primitives/#button)

## Callout

**Use it for:** An aside on a `Surface`: a titled block with a tone and the icon that tone implies.

```ts
import Callout from '@ambiqai/helia-ui/astro/Callout';
```

| Prop           | Type                                                                                                                             | Default  | Description                                                           |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------- |
| `title` (req.) | `string`                                                                                                                         | --       | The heading of the aside. Required: an untitled aside is a paragraph. |
| `tone`         | `\| 'note' \| 'tip' \| 'success' \| 'important' \| 'warning' \| 'critical' \| 'compatibility' \| 'experimental' \| 'deprecated'` | `'note'` | What the aside means. Picks the tint and the icon together.           |

**Slots**

- `default` -- The body of the aside. Prose, not a bare sentence fragment.

[See it rendered on Callouts](/helia-ui/callouts/#core-guidance)

## Card

**Use it for:** The card container: a `Surface` that carries no padding of its own.

```ts
import Card from '@ambiqai/helia-ui/astro/Card';
```

| Prop          | Type                                                                              | Default     | Description                                                                   |
| ------------- | --------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------- |
| `as`          | `Exclude<HTMLTag, 'a'>`                                                           | `'div'`     | Element to render. Never an anchor: the whole-card link is `CardHeader href`. |
| `tone`        | `'card' \| 'muted' \| 'paper' \| 'outlined' \| 'filled' \| 'elevated' \| 'ghost'` | `'card'`    | What stands in for the card's edge and fill.                                  |
| `media`       | `'top' \| 'side' \| 'background'`                                                 | `'top'`     | Where a slotted `CardMedia` sits. `side` stacks again below 42rem.            |
| `density`     | `'default' \| 'compact' \| 'comfortable'`                                         | `'default'` | Moves the padding step the parts default to: 3, 4, or 5.                      |
| `lift`        | `boolean`                                                                         | `false`     | Adds the hover lift. Pair it with `interactive` only when the card links.     |
| `interactive` | `boolean`                                                                         | `false`     | Adds the shared hover background shift and the focus-visible ring.            |

Also accepts `HTMLAttributes<'div'>`.

**Slots**

- `default` -- The card parts, in the order they should stack.

[See it rendered on Cards](/helia-ui/cards/#the-card-parts)

## CardActions

**Use it for:** The action row of a card: a flex row around whatever the consumer hands it — a `Button`, a `Chip`, a plain link, a pair of them.

```ts
import CardActions from '@ambiqai/helia-ui/astro/CardActions';
```

| Prop      | Type                            | Default   | Description                                                                                                    |
| --------- | ------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------- |
| `align`   | `'start' \| 'end' \| 'between'` | `'start'` | Row alignment. `between` pushes the first and last child apart.                                                |
| `padding` | `PaddingStep`                   | `'card'`  | Inner padding, named by spacing step. `card` follows the density dial; `none` leaves the part to own its gaps. |

Also accepts `HTMLAttributes<'div'>`.

**Slots**

- `default` -- The actions, in reading order.

[See it rendered on Cards](/helia-ui/cards/#a-card-with-every-part)

## CardContent

**Use it for:** The body of a card: a padding region around whatever the consumer writes.

```ts
import CardContent from '@ambiqai/helia-ui/astro/CardContent';
```

| Prop      | Type                 | Default   | Description                                                                                                    |
| --------- | -------------------- | --------- | -------------------------------------------------------------------------------------------------------------- |
| `padding` | `PaddingStep`        | `'card'`  | Inner padding, named by spacing step. `card` follows the density dial; `none` leaves the part to own its gaps. |
| `variant` | `'prose' \| 'stats'` | `'prose'` | `stats` lays the slotted figures out as a row that wraps rather than as prose.                                 |

Also accepts `HTMLAttributes<'div'>`.

**Slots**

- `default` -- The card's prose, or the figures when `variant` is `stats`.

[See it rendered on Cards](/helia-ui/cards/#a-card-with-every-part)

## CardHeader

**Use it for:** The title block of a card: an optional overline, the title, optional metadata under it, and an optional trailing action.

```ts
import CardHeader from '@ambiqai/helia-ui/astro/CardHeader';
```

| Prop       | Type                     | Default     | Description                                                                                                                                                                                                                                                                                        |
| ---------- | ------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `titleAs`  | `'h2' \| 'h3' \| 'h4'`   | `'h3'`      | Heading level for the title. Pick the one the page outline needs.                                                                                                                                                                                                                                  |
| `padding`  | `PaddingStep`            | `'card'`    | Inner padding, named by spacing step. `card` follows the density dial; `none` leaves the part to own its gaps.                                                                                                                                                                                     |
| `href`     | `string`                 | --          | Links the title, and stretches that link over the whole card.                                                                                                                                                                                                                                      |
| `overline` | `string`                 | --          | The overline text. Prefer this to the `overline` slot in MDX: MDX merges a slotted element and the title text that follows it into one paragraph, which drops the `slot` attribute and folds the overline into the heading's accessible name. The slot stays for markup an attribute cannot carry. |
| `step`     | `number \| string`       | --          | The position in an ordered flow, rendered as a numeral in the leading disc.                                                                                                                                                                                                                        |
| `scale`    | `'default' \| 'display'` | `'default'` | `display` takes the title and overline up a step, for a card standing in for a hero.                                                                                                                                                                                                               |

Also accepts `HTMLAttributes<'div'>`.

**Slots**

- `default` -- The title text. Omit it and no heading is rendered.
- `icon` -- A glyph for the tonal disc before the title, usually an `Icon`.
- `overline` -- The label above the title, for markup `overline` cannot carry.
- `meta` -- Metadata under the title, such as a date or an author.
- `action` -- A trailing control, aligned to the top right of the header.

[See it rendered on Cards](/helia-ui/cards/#a-card-with-every-part)

## CardList

**Use it for:** A short list inside a card: what a plan includes, what a guide covers, the order a flow runs in.

```ts
import CardList from '@ambiqai/helia-ui/astro/CardList';
```

| Prop           | Type                              | Default   | Description                                                                                                    |
| -------------- | --------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------- |
| `items` (req.) | `string[]`                        | --        | The lines, in reading order. Plain text: an item needing markup is a composition.                              |
| `marker`       | `'check' \| 'bullet' \| 'number'` | `'check'` | What sits before each line. `number` renders an ordered list.                                                  |
| `icon`         | `IconName`                        | --        | Replaces the glyph on a `check` or `bullet` list. Ignored when `marker` is `number`.                           |
| `padding`      | `PaddingStep`                     | `'card'`  | Inner padding, named by spacing step. `card` follows the density dial; `none` leaves the part to own its gaps. |

Also accepts `HTMLAttributes<'ul'>`.

**Slots** -- none.

[See it rendered on Gallery](/helia-ui/gallery/#content-first)

## CardMedia

**Use it for:** The media region of a card: a fixed-ratio frame around whatever the consumer puts in it — an `<img>`, a `<Media>`, an inline SVG, a video poster.

```ts
import CardMedia from '@ambiqai/helia-ui/astro/CardMedia';
```

| Prop    | Type                       | Default   | Description                        |
| ------- | -------------------------- | --------- | ---------------------------------- |
| `ratio` | `'16/9' \| '4/3' \| '1/1'` | `'16/9'`  | Aspect ratio of the frame.         |
| `fit`   | `'cover' \| 'contain'`     | `'cover'` | How slotted media fills the frame. |

Also accepts `HTMLAttributes<'div'>`.

**Slots**

- `default` -- The media. An `<img>`, a `<Media>`, an inline SVG, a poster.

[See it rendered on Cards](/helia-ui/cards/#a-card-with-every-part)

## CardQuote

**Use it for:** A quotation as a card part: the line, who said it, and an optional initials mark where a photograph would otherwise have to be found.

```ts
import CardQuote from '@ambiqai/helia-ui/astro/CardQuote';
```

| Prop          | Type          | Default  | Description                                                                                                    |
| ------------- | ------------- | -------- | -------------------------------------------------------------------------------------------------------------- |
| `attribution` | `string`      | --       | Who said it. Rendered beside the mark.                                                                         |
| `detail`      | `string`      | --       | The line under the attribution: a role, a team, a release.                                                     |
| `initials`    | `string`      | --       | One or two letters standing in for a portrait. Decorative.                                                     |
| `padding`     | `PaddingStep` | `'card'` | Inner padding, named by spacing step. `card` follows the density dial; `none` leaves the part to own its gaps. |

Also accepts `HTMLAttributes<'figure'>`.

**Slots**

- `default` -- The quotation. Keep it inline so MDX leaves it phrasing.
- `attribution` -- Attribution markup an attribute cannot carry, such as a link.

[See it rendered on Gallery](/helia-ui/gallery/#content-first)

## Chip

**Use it for:** The pill: a rounded inline label or link.

```ts
import Chip from '@ambiqai/helia-ui/astro/Chip';
```

| Prop   | Type                                | Default     | Description                                                                                                                                     |
| ------ | ----------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `tone` | `'neutral' \| 'accent' \| 'static'` | `'neutral'` | `neutral` is the default outlined pill and reacts to pointer state when it links; `accent` is the filled badge; `static` is read-only metadata. |
| `size` | `'sm' \| 'md'`                      | `'sm'`      | `sm` is the metadata size, `md` the standalone one.                                                                                             |
| `href` | `string`                            | --          | Where it goes. Its presence is what makes this an anchor.                                                                                       |

Also accepts `Omit<HTMLAttributes<'a'>, 'href'>`, `HTMLAttributes<'span'>`.

**Slots**

- `default` -- The label, and the link text when `href` is set.

[See it rendered on Primitives](/helia-ui/primitives/#chip)

## CodeBlock

**Use it for:** A code sample rendered through Expressive Code: the highlighted source in a frame, with the frame's own copy control.

```ts
import CodeBlock from '@ambiqai/helia-ui/astro/CodeBlock';
```

| Prop             | Type                                                                                                        | Default       | Description                                                                                                                                         |
| ---------------- | ----------------------------------------------------------------------------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| `code` (req.)    | `string`                                                                                                    | --            | The source, verbatim. It is also what the copy control puts on the clipboard.                                                                       |
| `language`       | `\| 'bash' \| 'c' \| 'cpp' \| 'diff' \| 'json' \| 'plaintext' \| 'python' \| 'sh' \| 'tsx' \| 'typescript'` | `'plaintext'` | Grammar to highlight with.                                                                                                                          |
| `filename`       | `string`                                                                                                    | --            | Path shown in the frame's title. Takes precedence over `label`.                                                                                     |
| `label`          | `string`                                                                                                    | --            | Frame title when there is no `filename`. With neither, the frame carries no visible title; a terminal frame is still named to assistive technology. |
| `highlightLines` | `readonly number[]`                                                                                         | `[]`          | 1-based line numbers to mark as changed or worth reading first.                                                                                     |
| `lineNumbers`    | `boolean`                                                                                                   | `false`       | Renders the line-number gutter.                                                                                                                     |
| `variant`        | `'bare' \| 'code' \| 'terminal'`                                                                            | `'code'`      | `code` is the framed default, `terminal` the shell form, `bare` drops the frame, leaving the highlighted source on its own.                         |
| `class`          | `string`                                                                                                    | --            | Forwarded to the frame, so a layout can size it without reaching inside.                                                                            |

**Slots** -- none.

[See it rendered on Code](/helia-ui/code/#file-example)

## CodeTabs

**Use it for:** One code sample shown several ways: a tab per language or per toolchain, with only the selected panel visible.

```ts
import CodeTabs from '@ambiqai/helia-ui/astro/CodeTabs';
```

| Prop          | Type                 | Default           | Description                                                              |
| ------------- | -------------------- | ----------------- | ------------------------------------------------------------------------ |
| `tabs` (req.) | `readonly CodeTab[]` | --                | The samples, in tab order. The first one renders selected.               |
| `ariaLabel`   | `string`             | `'Code examples'` | Accessible name for the tab list or the select.                          |
| `variant`     | `'tabs' \| 'select'` | `'tabs'`          | `tabs` is the row of tabs; `select` is the dropdown for a narrow column. |

**CodeTab**

| Prop              | Type                                                                                              | Default | Description                                                         |
| ----------------- | ------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------- |
| `label` (req.)    | `string`                                                                                          | --      | The tab's text, and how the panel is named to assistive technology. |
| `language` (req.) | `\| 'bash' \| 'c' \| 'cpp' \| 'json' \| 'plaintext' \| 'python' \| 'sh' \| 'tsx' \| 'typescript'` | --      | Grammar to highlight this tab's source with.                        |
| `code` (req.)     | `string`                                                                                          | --      | The source for this tab, verbatim.                                  |

**Slots** -- none.

[See it rendered on Code](/helia-ui/code/#language-tabs)

## DataTable

**Use it for:** A scrollable data table with an optional client-side pager.

```ts
import DataTable from '@ambiqai/helia-ui/astro/DataTable';
```

| Prop             | Type                             | Default | Description                                                          |
| ---------------- | -------------------------------- | ------- | -------------------------------------------------------------------- |
| `columns` (req.) | `readonly string[]`              | --      | Header cells, left to right. Sets the column count.                  |
| `rows` (req.)    | `readonly (readonly string[])[]` | --      | Body rows, each one cell per column, in column order.                |
| `caption` (req.) | `string`                         | --      | The table's caption, and the accessible name of its pager. Required. |
| `pageSize`       | `number`                         | `0`     | Rows per page. 0 renders every row and no pager.                     |

**Slots** -- none.

[See it rendered on Layout](/helia-ui/layout/#data-table)

## EditorialBand

**Use it for:** A full-bleed band across the content column: the paint reaches the viewport edges while the text stays on the readable measure.

```ts
import EditorialBand from '@ambiqai/helia-ui/astro/EditorialBand';
```

| Prop   | Type                            | Default  | Description                                                                                |
| ------ | ------------------------------- | -------- | ------------------------------------------------------------------------------------------ |
| `tone` | `'base' \| 'paper' \| 'subtle'` | `'base'` | The ground. `base` is the page background; `paper` and `subtle` both paint the paper fill. |
| `id`   | `string`                        | --       | Anchor for the section, so a nav or a deep link can reach it.                              |

**Slots**

- `default` -- The band's content, held to the readable width.

[See it rendered on Layout](/helia-ui/layout/#editorial-band)

## Eyebrow

**Use it for:** The uppercase label that sits above a heading.

```ts
import Eyebrow from '@ambiqai/helia-ui/astro/Eyebrow';
```

| Prop | Type      | Default | Description                                                                  |
| ---- | --------- | ------- | ---------------------------------------------------------------------------- |
| `as` | `HTMLTag` | `'p'`   | Element to render. Use a `<span>` where a paragraph would break the outline. |

Also accepts `HTMLAttributes<'p'>`.

**Slots**

- `default` -- The label text.

[See it rendered on Primitives](/helia-ui/primitives/#eyebrow)

## Icon

**Use it for:** A named icon from Font Awesome's free solid set, resolved at build time.

```ts
import Icon from '@ambiqai/helia-ui/astro/Icon';
```

| Prop          | Type       | Default | Description                                                                                                                                            |
| ------------- | ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `name` (req.) | `IconName` | --      | Font Awesome solid name in kebab case, e.g. `gauge-high`.                                                                                              |
| `label`       | `string`   | --      | Accessible name. Omit it for decoration next to text that already says the same thing; supply it when the icon is the only thing carrying the meaning. |

Also accepts `HTMLAttributes<'span'>`.

**Slots** -- none.

[See it rendered on Primitives](/helia-ui/primitives/#icon)

## LinkCard

**Use it for:** A whole card that is one link: a title, a line about it, and a cue that moves when the card is under the pointer.

```ts
import LinkCard from '@ambiqai/helia-ui/astro/LinkCard';
```

| Prop           | Type                                      | Default | Description                                                             |
| -------------- | ----------------------------------------- | ------- | ----------------------------------------------------------------------- |
| `href` (req.)  | `string`                                  | --      | Where the card goes.                                                    |
| `title` (req.) | `string`                                  | --      | The link text, and the card's accessible name.                          |
| `titleAs`      | `'h2' \| 'h3' \| 'h4'`                    | `'h3'`  | Heading level. Pick the one the page outline needs.                     |
| `overline`     | `string`                                  | --      | The label above the title.                                              |
| `tone`         | `CardTone`                                | --      | What stands in for the card's edge and fill. Passed straight to `Card`. |
| `density`      | `'default' \| 'compact' \| 'comfortable'` | --      | Moves the padding step the parts default to. Passed straight to `Card`. |
| `lift`         | `boolean`                                 | --      | Adds the hover lift. Passed straight to `Card`.                         |

Also accepts `Omit<HTMLAttributes<'div'>, 'title'>`.

**Slots**

- `default` -- The line of description under the title.

[See it rendered on Gallery](/helia-ui/gallery/#linkcard)

## Media

**Use it for:** Generative artwork for a card that has no real image: oversized blurred forms, or a sparse field of planes, orbits and wires.

```ts
import Media from '@ambiqai/helia-ui/astro/Media';
```

| Prop        | Type                                               | Default         | Description                                                    |
| ----------- | -------------------------------------------------- | --------------- | -------------------------------------------------------------- |
| `treatment` | `'color-field' \| 'graphite' \| 'slate' \| 'wire'` | `'color-field'` | Which artwork the surface draws.                               |
| `ratio`     | `'16/9' \| '4/3' \| '1/1'`                         | --              | Aspect ratio. Omit inside a `CardMedia`, which owns the frame. |

Also accepts `HTMLAttributes<'div'>`.

**Slots** -- none.

[See it rendered on Media](/helia-ui/media/#generative-artwork)

## MediaEmbed

**Use it for:** A hosted video, held behind its poster until someone asks for it.

```ts
import MediaEmbed from '@ambiqai/helia-ui/astro/MediaEmbed';
```

| Prop             | Type     | Default                                            | Description                                                                                                                                                   |
| ---------------- | -------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `title` (req.)   | `string` | --                                                 | What the video is. Names the frame, the play control, and the fallback link.                                                                                  |
| `videoId` (req.) | `string` | --                                                 | The YouTube video ID, not a URL. Validated at build.                                                                                                          |
| `posterSrc`      | `string` | --                                                 | Poster image. A bare path is resolved against the site's base; an absolute path, a URL, or a data URI is used as given. Omit it for the abstract placeholder. |
| `posterAlt`      | `string` | `''`                                               | Alternative text for the poster. Leave it empty where the title already says the same thing.                                                                  |
| `caption`        | `string` | --                                                 | Caption under the figure. Omit it and no figcaption renders.                                                                                                  |
| `duration`       | `string` | --                                                 | Running time, shown over the poster. Free text, for example `4:12`.                                                                                           |
| `externalUrl`    | `string` | `` `https://www.youtube.com/watch?v=${videoId}` `` | Where the fallback and caption links go. Defaults to the watch URL for `videoId`.                                                                             |
| `class`          | `string` | --                                                 | Forwarded to the figure, so a layout can size it without reaching inside.                                                                                     |

**Slots** -- none.

[See it rendered on Media](/helia-ui/media/#hosted-video)

## Reveal

**Use it for:** Entry animation for whatever it wraps: a short fade and rise the first time the element reaches the viewport.

```ts
import Reveal from '@ambiqai/helia-ui/astro/Reveal';
```

| Prop        | Type      | Default | Description                                                                                                                                                                                               |
| ----------- | --------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `threshold` | `number`  | `0.15`  | How much of the element has to be in view before it reveals, 0 to 1. Lower it for a tall section that would otherwise never cross the mark.                                                               |
| `stagger`   | `boolean` | `false` | Cascade the direct children instead of revealing the wrapper as one block: each child waits one `--helia-motion-fast` step longer than the one before it, to a ceiling of six. Use it on a grid of cards. |

Also accepts `HTMLAttributes<'div'>`.

**Slots**

- `default` -- The content to reveal. Visible without JavaScript.

[See it rendered on Gallery](/helia-ui/gallery/#reveal-on-scroll)

## SectionHeader

**Use it for:** The heading of a page section: an optional label, the title, an optional line under it, and an optional link to the fuller view.

```ts
import SectionHeader from '@ambiqai/helia-ui/astro/SectionHeader';
```

| Prop           | Type     | Default      | Description                                                |
| -------------- | -------- | ------------ | ---------------------------------------------------------- |
| `eyebrow`      | `string` | --           | The uppercase label above the title.                       |
| `title` (req.) | `string` | --           | The section's heading. Required.                           |
| `description`  | `string` | --           | One line about the section, under the title.               |
| `href`         | `string` | --           | Where the trailing link goes. Without it, no link renders. |
| `linkLabel`    | `string` | `'View all'` | The trailing link's text. Ignored without `href`.          |

**Slots** -- none.

[See it rendered on Layout](/helia-ui/layout/#section-header)

## ShowcaseCarousel

**Use it for:** The card rail: a scroll-snapping track of whatever cards the consumer slots in, with one tab per card and previous/next controls.

```ts
import ShowcaseCarousel from '@ambiqai/helia-ui/astro/ShowcaseCarousel';
```

| Prop          | Type                     | Default     | Description                                                                                                                                                                                                         |
| ------------- | ------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tabs` (req.) | `readonly string[]`      | --          | One label per card, in the order the cards are slotted.                                                                                                                                                             |
| `variant`     | `'feature' \| 'compact'` | `'feature'` | `feature` is the large-media default used by the demos and catalog pages. `compact` is the text-forward variant: a short media band above eyebrow, title, description, and date, all inside a single bordered card. |

**Slots**

- `default` -- The cards, in the same order as `tabs`.

[See it rendered on Layout](/helia-ui/layout/#showcase-carousel)

## StatCard

**Use it for:** A single figure with its label, and an optional change beside it.

```ts
import StatCard from '@ambiqai/helia-ui/astro/StatCard';
```

| Prop           | Type                                                                    | Default     | Description                                                                 |
| -------------- | ----------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------- |
| `label` (req.) | `string`                                                                | --          | What the figure counts. Rendered as the eyebrow above it.                   |
| `delta`        | `string`                                                                | --          | The change beside the figure. Omit it and no badge renders.                 |
| `deltaTone`    | `'neutral' \| 'info' \| 'success' \| 'warning' \| 'danger' \| 'accent'` | `'neutral'` | Which way the change reads. The component does not decide.                  |
| `tone`         | `CardTone`                                                              | --          | What stands in for the card's edge and fill. Passed straight to `Card`.     |
| `density`      | `'default' \| 'compact' \| 'comfortable'`                               | --          | Moves the padding step the parts default to. Passed straight to `Card`.     |
| `lift`         | `boolean`                                                               | --          | Adds the hover lift. Passed straight to `Card`.                             |
| `interactive`  | `boolean`                                                               | --          | Adds the hover shift and the focus-visible ring. Passed straight to `Card`. |

Also accepts `HTMLAttributes<'div'>`.

**Slots**

- `default` -- The figure itself, with whatever unit or markup it needs.

[See it rendered on Gallery](/helia-ui/gallery/#statcard)

## Surface

**Use it for:** The card surface: hairline border, shared radius, one of three backgrounds.

```ts
import Surface from '@ambiqai/helia-ui/astro/Surface';
```

| Prop          | Type                                                                 | Default  | Description                                                        |
| ------------- | -------------------------------------------------------------------- | -------- | ------------------------------------------------------------------ |
| `as`          | `HTMLTag`                                                            | `'div'`  | Element to render. Ignored when `href` is set.                     |
| `tone`        | `'card' \| 'muted' \| 'paper'`                                       | `'card'` | Background: the raised card, the recessed muted fill, or paper.    |
| `padding`     | `'none' \| 'card' \| 'px' \| '1' \| '2' \| '3' \| '4' \| '5' \| '6'` | `'4'`    | Inner padding, named by spacing step.                              |
| `interactive` | `boolean`                                                            | `false`  | Adds the shared hover background shift and the focus-visible ring. |
| `href`        | `string`                                                             | --       | Renders an `<a>` regardless of `as`.                               |

Also accepts `HTMLAttributes<'div'>`.

**Slots**

- `default` -- Whatever the surface holds.

[See it rendered on Primitives](/helia-ui/primitives/#surface)

## Timeline

**Use it for:** A dated list of grouped entries: releases, changes, or planned work.

```ts
import Timeline from '@ambiqai/helia-ui/astro/Timeline';
```

| Prop            | Type                     | Default                 | Description                                                              |
| --------------- | ------------------------ | ----------------------- | ------------------------------------------------------------------------ |
| `groups` (req.) | `TimelineGroup[]`        | --                      | The groups, in display order.                                            |
| `heading`       | `string`                 | --                      | Heading above the rail. Omit it where the page already has one.          |
| `actionLabel`   | `string`                 | --                      | Text of the trailing link in the header. Needs `actionHref` to render.   |
| `actionHref`    | `string`                 | --                      | Where the trailing link goes. Needs `actionLabel` to render.             |
| `ariaLabel`     | `string`                 | `heading ?? 'Timeline'` | Accessible name for the rail. Defaults to `heading`, then to `Timeline`. |
| `variant`       | `'updates' \| 'roadmap'` | `'updates'`             | `updates` reads as history, `roadmap` as planned work.                   |

**TimelineGroup**

| Prop           | Type                                                               | Default | Description                                                             |
| -------------- | ------------------------------------------------------------------ | ------- | ----------------------------------------------------------------------- |
| `label` (req.) | `string`                                                           | --      | The group's marker text, usually a date or a milestone.                 |
| `datetime`     | `string`                                                           | --      | Machine-readable date for the label, as an HTML `datetime` value.       |
| `tone`         | `'neutral' \| 'blue' \| 'cyan' \| 'green' \| 'orange' \| 'purple'` | --      | Advisory tint for the group's marker. The caller decides what it means. |
| `items` (req.) | `TimelineItem[]`                                                   | --      | The group's entries, in display order.                                  |

**Slots** -- none.

[See it rendered on Timeline](/helia-ui/timeline/#recent-updates)
