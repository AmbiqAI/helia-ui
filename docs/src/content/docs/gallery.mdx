---
title: Gallery
description: Every card variant, every named transition, and the site theme dials, side by side.
---

import AccordionGroup from '@ambiqai/helia-ui/astro/AccordionGroup';
import AsciiTerminal from '@ambiqai/helia-ui/astro/AsciiTerminal';
import Badge from '@ambiqai/helia-ui/astro/Badge';
import Band from '@ambiqai/helia-ui/astro/Band';
import Button from '@ambiqai/helia-ui/astro/Button';
import Callout from '@ambiqai/helia-ui/astro/Callout';
import Card from '@ambiqai/helia-ui/astro/Card';
import CardActions from '@ambiqai/helia-ui/astro/CardActions';
import CardContent from '@ambiqai/helia-ui/astro/CardContent';
import CardGrid from '@ambiqai/helia-ui/astro/CardGrid';
import CardHeader from '@ambiqai/helia-ui/astro/CardHeader';
import CardList from '@ambiqai/helia-ui/astro/CardList';
import CardMedia from '@ambiqai/helia-ui/astro/CardMedia';
import CardQuote from '@ambiqai/helia-ui/astro/CardQuote';
import Chart from '@ambiqai/helia-ui/astro/Chart';
import ChartGroup from '@ambiqai/helia-ui/astro/ChartGroup';
import Chip from '@ambiqai/helia-ui/astro/Chip';
import CodeTabs from '@ambiqai/helia-ui/astro/CodeTabs';
import DataTable from '@ambiqai/helia-ui/astro/DataTable';
import EditorialBand from '@ambiqai/helia-ui/astro/EditorialBand';
import Example from '../../components/Example.astro';
import Eyebrow from '@ambiqai/helia-ui/astro/Eyebrow';
import Icon from '@ambiqai/helia-ui/astro/Icon';
import LinkCard from '@ambiqai/helia-ui/astro/LinkCard';
import Masonry from '@ambiqai/helia-ui/astro/Masonry';
import Media from '@ambiqai/helia-ui/astro/Media';
import MediaEmbed from '@ambiqai/helia-ui/astro/MediaEmbed';
import Mosaic from '@ambiqai/helia-ui/astro/Mosaic';
import Reveal from '@ambiqai/helia-ui/astro/Reveal';
import RevealReplay from '../../components/RevealReplay.astro';
import SectionHeader from '@ambiqai/helia-ui/astro/SectionHeader';
import ShowcaseCarousel from '@ambiqai/helia-ui/astro/ShowcaseCarousel';
import StatCard from '@ambiqai/helia-ui/astro/StatCard';
import Timeline from '@ambiqai/helia-ui/astro/Timeline';
import {
  buildSeries,
  pagesSeries,
  trafficSeries,
  weightSeries,
} from '../../lib/chart-gallery-data';

This page is for choosing. Every option the card parts and the motion system
offer is on it, side by side, each one showing the exact source that produces
it behind "Show code". Nothing here is a new component: the variants are
modifiers on `Card` and the transitions are classes in `recipes.css`.

The reference tables are on [Cards](/helia-ui/cards/) for the parts and in
`docs/design-system.md` for the motion tokens and the site theme.

## Tone

Four tones, each of them a different answer to "what stands in for the edge".
`outlined` is the default and `card` is its older name; `filled` and `muted`
are likewise the same tone. Padding and radius do not move between them, so a
row can mix tones and still line up.

<div className="gallery-grid gallery-grid--wide not-content">
  <Example
    title='tone="elevated"'
    code={`
      <Card tone="elevated">
        <CardContent>A shadow instead of a border.</CardContent>
      </Card>
    `}
  >
    <Card tone="elevated">
      <CardContent>A shadow instead of a border.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="outlined"'
    code={`
      <Card tone="outlined">
        <CardContent>The hairline card, unchanged.</CardContent>
      </Card>
    `}
  >
    <Card tone="outlined">
      <CardContent>The hairline card, unchanged.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="filled"'
    code={`
      <Card tone="filled">
        <CardContent>The muted fill, for a card inside a surface.</CardContent>
      </Card>
    `}
  >
    <Card tone="filled">
      <CardContent>The muted fill, for a card inside a surface.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="ghost"'
    code={`
      <Card tone="ghost">
        <CardContent>Nothing at rest. Point at it for the fill.</CardContent>
      </Card>
    `}
  >
    <Card tone="ghost">
      <CardContent>Nothing at rest. Point at it for the fill.</CardContent>
    </Card>
  </Example>
</div>

## Tone and density together

`density` moves the step the card parts pad to: 3 for `compact`, 4 by default,
5 for `comfortable`. A part that names its own step opts out and keeps it.

### Compact

<div className="gallery-grid gallery-grid--wide not-content">
  <Example
    title='tone="elevated" density="compact"'
    code={`
      <Card tone="elevated" density="compact">
        <CardContent>Elevated, compact.</CardContent>
      </Card>
    `}
  >
    <Card tone="elevated" density="compact">
      <CardContent>Elevated, compact.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="outlined" density="compact"'
    code={`
      <Card tone="outlined" density="compact">
        <CardContent>Outlined, compact.</CardContent>
      </Card>
    `}
  >
    <Card tone="outlined" density="compact">
      <CardContent>Outlined, compact.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="filled" density="compact"'
    code={`
      <Card tone="filled" density="compact">
        <CardContent>Filled, compact.</CardContent>
      </Card>
    `}
  >
    <Card tone="filled" density="compact">
      <CardContent>Filled, compact.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="ghost" density="compact"'
    code={`
      <Card tone="ghost" density="compact">
        <CardContent>Ghost, compact.</CardContent>
      </Card>
    `}
  >
    <Card tone="ghost" density="compact">
      <CardContent>Ghost, compact.</CardContent>
    </Card>
  </Example>
</div>

### Default

<div className="gallery-grid gallery-grid--wide not-content">
  <Example
    title='tone="elevated"'
    code={`
      <Card tone="elevated">
        <CardContent>Elevated, default.</CardContent>
      </Card>
    `}
  >
    <Card tone="elevated">
      <CardContent>Elevated, default.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="outlined"'
    code={`
      <Card tone="outlined">
        <CardContent>Outlined, default.</CardContent>
      </Card>
    `}
  >
    <Card tone="outlined">
      <CardContent>Outlined, default.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="filled"'
    code={`
      <Card tone="filled">
        <CardContent>Filled, default.</CardContent>
      </Card>
    `}
  >
    <Card tone="filled">
      <CardContent>Filled, default.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="ghost"'
    code={`
      <Card tone="ghost">
        <CardContent>Ghost, default.</CardContent>
      </Card>
    `}
  >
    <Card tone="ghost">
      <CardContent>Ghost, default.</CardContent>
    </Card>
  </Example>
</div>

### Comfortable

<div className="gallery-grid gallery-grid--wide not-content">
  <Example
    title='tone="elevated" density="comfortable"'
    code={`
      <Card tone="elevated" density="comfortable">
        <CardContent>Elevated, comfortable.</CardContent>
      </Card>
    `}
  >
    <Card tone="elevated" density="comfortable">
      <CardContent>Elevated, comfortable.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="outlined" density="comfortable"'
    code={`
      <Card tone="outlined" density="comfortable">
        <CardContent>Outlined, comfortable.</CardContent>
      </Card>
    `}
  >
    <Card tone="outlined" density="comfortable">
      <CardContent>Outlined, comfortable.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="filled" density="comfortable"'
    code={`
      <Card tone="filled" density="comfortable">
        <CardContent>Filled, comfortable.</CardContent>
      </Card>
    `}
  >
    <Card tone="filled" density="comfortable">
      <CardContent>Filled, comfortable.</CardContent>
    </Card>
  </Example>
  <Example
    title='tone="ghost" density="comfortable"'
    code={`
      <Card tone="ghost" density="comfortable">
        <CardContent>Ghost, comfortable.</CardContent>
      </Card>
    `}
  >
    <Card tone="ghost" density="comfortable">
      <CardContent>Ghost, comfortable.</CardContent>
    </Card>
  </Example>
</div>

## Media placement

`top` is the stacked card. `side` puts the frame down the leading edge at 40%
of the card and stacks again below 42rem, so a narrow viewport gets the top
placement whatever is asked for. `background` fills the card with the frame,
draws an overlay over it, and inverts the ink onto the fixed paper tone.

### The three placements

<div className="gallery-grid gallery-grid--wide not-content">
  <Example
    title="<Card> (media on top)"
    code={`
      <Card>
        <CardMedia ratio="16/9">
          <Media
            treatment="color-field"
            style="--accent: var(--helia-accent-purple)"
          />
        </CardMedia>
        <CardHeader titleAs="h4">
          <span>Media on top</span>
        </CardHeader>
        <CardContent>The frame owns the ratio and the card stacks.</CardContent>
      </Card>
    `}
  >
    <Card>
      <CardMedia ratio="16/9">
        <Media
          treatment="color-field"
          style="--accent: var(--helia-accent-purple)"
        />
      </CardMedia>
      <CardHeader titleAs="h4">
        <span>Media on top</span>
      </CardHeader>
      <CardContent>The frame owns the ratio and the card stacks.</CardContent>
    </Card>
  </Example>
  <Example
    title='<Card media="side">'
    code={`
      <Card media="side">
        <CardMedia ratio="16/9">
          <Media
            treatment="graphite"
            style="--accent: var(--helia-accent-cyan)"
          />
        </CardMedia>
        <CardHeader titleAs="h4">
          <span>Media beside</span>
        </CardHeader>
        <CardContent>
          The frame leaves the flow and the card reserves its width, so the
          header, the body and the actions stay stacked.
        </CardContent>
      </Card>
    `}
  >
    <Card media="side">
      <CardMedia ratio="16/9">
        <Media
          treatment="graphite"
          style="--accent: var(--helia-accent-cyan)"
        />
      </CardMedia>
      <CardHeader titleAs="h4">
        <span>Media beside</span>
      </CardHeader>
      <CardContent>
        The frame leaves the flow and the card reserves its width, so the
        header, the body and the actions stay stacked.
      </CardContent>
    </Card>
  </Example>
  <Example
    title='<Card media="background">'
    code={`
      <Card media="background">
        <CardMedia>
          <Media
            treatment="color-field"
            style="--accent: var(--helia-accent-blue)"
          />
        </CardMedia>
        <CardHeader titleAs="h4">
          <span>Media behind</span>
        </CardHeader>
        <CardContent>
          The ink inverts to the fixed paper tone, because the overlay is dark
          in both themes.
        </CardContent>
      </Card>
    `}
  >
    <Card media="background">
      <CardMedia>
        <Media
          treatment="color-field"
          style="--accent: var(--helia-accent-blue)"
        />
      </CardMedia>
      <CardHeader titleAs="h4">
        <span>Media behind</span>
      </CardHeader>
      <CardContent>
        The ink inverts to the fixed paper tone, because the overlay is dark in
        both themes.
      </CardContent>
    </Card>
  </Example>
</div>

## StatCard

A figure, its label, and an optional change. The number is the child, so it can
carry a unit or markup; the delta is a `Badge` and its tone is the caller's,
because the component has no idea whether up is good.

<div className="gallery-grid gallery-grid--wide not-content">
  <Example
    title='<StatCard label="Routes emitted">'
    code={`
      <StatCard label="Routes emitted">206</StatCard>
    `}
  >
    <StatCard label="Routes emitted">206</StatCard>
  </Example>
  <Example
    title='delta="-18%" deltaTone="success"'
    code={`
      <StatCard label="Build time" delta="-18%" deltaTone="success">
        <span>41s</span>
      </StatCard>
    `}
  >
    <StatCard label="Build time" delta="-18%" deltaTone="success">
      <span>41s</span>
    </StatCard>
  </Example>
  <Example
    title='deltaTone="warning"'
    code={`
      <StatCard label="Open findings" delta="+3" deltaTone="warning">
        <span>12</span>
      </StatCard>
    `}
  >
    <StatCard label="Open findings" delta="+3" deltaTone="warning">
      <span>12</span>
    </StatCard>
  </Example>
  <Example
    title='tone="elevated" density="compact"'
    code={`
      <StatCard label="Bundle" tone="elevated" density="compact">
        <span>84 kB</span>
      </StatCard>
    `}
  >
    <StatCard label="Bundle" tone="elevated" density="compact">
      <span>84 kB</span>
    </StatCard>
  </Example>
</div>

## LinkCard

Title, one line, and a cue that moves when the card is under the pointer. The
link lives on the title and the recipe stretches it over the card, so the whole
card is the target and the cue is inside the same hit area.

### Three link cards

<div className="gallery-grid gallery-grid--wide not-content">
  <Example
    title="<LinkCard href title>"
    code={`
      <LinkCard titleAs="h4" href="/helia-ui/cards/" title="The card parts">
        What each part owns, and how a subset of them composes.
      </LinkCard>
    `}
  >
    <LinkCard titleAs="h4" href="/helia-ui/cards/" title="The card parts">
      What each part owns, and how a subset of them composes.
    </LinkCard>
  </Example>
  <Example
    title='overline="Tokens" tone="ghost"'
    code={`
      <LinkCard
        titleAs="h4"
        href="/helia-ui/foundations/"
        title="Foundations"
        overline="Tokens"
        tone="ghost"
      >
        The scales every part compiles against.
      </LinkCard>
    `}
  >
    <LinkCard
      titleAs="h4"
      href="/helia-ui/foundations/"
      title="Foundations"
      overline="Tokens"
      tone="ghost"
    >
      The scales every part compiles against.
    </LinkCard>
  </Example>
  <Example
    title='tone="elevated" lift'
    code={`
      <LinkCard
        titleAs="h4"
        href="/helia-ui/primitives/"
        title="Primitives"
        tone="elevated"
        lift
      >
        The four shapes the rest of the system reuses.
      </LinkCard>
    `}
  >
    <LinkCard
      titleAs="h4"
      href="/helia-ui/primitives/"
      title="Primitives"
      tone="elevated"
      lift
    >
      The four shapes the rest of the system reuses.
    </LinkCard>
  </Example>
</div>

## Content-first

Six cards that need no image. Good artwork is the scarcest thing a card family
depends on, so each of these carries itself on type, a mark or a figure
instead. None of them is a new component: they are the `CardHeader` marks and
scale, two parts (`CardList`, `CardQuote`) and one `CardContent` variant, so
tone, density and the lift reach all of them unchanged.

### Comfortable

<div
  className="gallery-grid gallery-grid--wide not-content"
  data-content-first="comfortable"
>
  <Example
    title='<CardHeader> with <Icon slot="icon">'
    code={`
      <Card density="comfortable">
        <CardHeader titleAs="h4" overline="Profiling">
          <Icon slot="icon" name="gauge-high" />
          <span>Latency you can budget</span>
        </CardHeader>
        <CardContent>
          Every kernel reports its own cycle count, so a regression has a name
          before it has a bug report.
        </CardContent>
      </Card>
    `}
  >
    <Card density="comfortable">
      <CardHeader titleAs="h4" overline="Profiling">
        <Icon slot="icon" name="gauge-high" />
        <span>Latency you can budget</span>
      </CardHeader>
      <CardContent>
        Every kernel reports its own cycle count, so a regression has a name
        before it has a bug report.
      </CardContent>
    </Card>
  </Example>
  <Example
    title="<CardList items={[...]} />"
    code={`
      <Card density="comfortable">
        <CardHeader titleAs="h4" overline="Included">
          <span>What the SDK ships with</span>
        </CardHeader>
        <CardList
          items={[
            'Quantisation-aware training recipes',
            'A profiler that reads the device counters',
            'Reference builds for every supported board',
          ]}
        />
      </Card>
    `}
  >
    <Card density="comfortable">
      <CardHeader titleAs="h4" overline="Included">
        <span>What the SDK ships with</span>
      </CardHeader>
      <CardList
        items={[
          'Quantisation-aware training recipes',
          'A profiler that reads the device counters',
          'Reference builds for every supported board',
        ]}
      />
    </Card>
  </Example>
  <Example
    title='<CardHeader step={2} overline="Step two">'
    code={`
      <Card density="comfortable">
        <CardHeader titleAs="h4" step={2} overline="Step two">
          <span>Quantise the graph</span>
        </CardHeader>
        <CardContent>
          The converter keeps the calibration set with the artefact, so the
          same numbers come back on the next run.
        </CardContent>
      </Card>
    `}
  >
    <Card density="comfortable">
      <CardHeader titleAs="h4" step={2} overline="Step two">
        <span>Quantise the graph</span>
      </CardHeader>
      <CardContent>
        The converter keeps the calibration set with the artefact, so the same
        numbers come back on the next run.
      </CardContent>
    </Card>
  </Example>
  <Example
    title="<CardQuote attribution detail initials>"
    code={`
      <Card density="comfortable">
        <CardQuote
          attribution="Dana Mercer"
          detail="Firmware lead"
          initials="DM"
        >
          The profile told us where the cycles went on the first afternoon,
          which is a week we did not spend guessing.
        </CardQuote>
      </Card>
    `}
  >
    <Card density="comfortable">
      <CardQuote attribution="Dana Mercer" detail="Firmware lead" initials="DM">
        The profile told us where the cycles went on the first afternoon, which
        is a week we did not spend guessing.
      </CardQuote>
    </Card>
  </Example>
  <Example
    title='<CardContent variant="stats">'
    code={`
      <Card density="comfortable">
        <CardHeader titleAs="h4" overline="Measured on device">
          <span>Keyword spotting</span>
        </CardHeader>
        <CardContent variant="stats">
          <StatCard label="Latency">18 ms</StatCard>
          <StatCard label="Energy">2.4 mJ</StatCard>
          <StatCard label="Model">96 kB</StatCard>
        </CardContent>
      </Card>
    `}
  >
    <Card density="comfortable">
      <CardHeader titleAs="h4" overline="Measured on device">
        <span>Keyword spotting</span>
      </CardHeader>
      <CardContent variant="stats">
        <StatCard label="Latency">18 ms</StatCard>
        <StatCard label="Energy">2.4 mJ</StatCard>
        <StatCard label="Model">96 kB</StatCard>
      </CardContent>
    </Card>
  </Example>
  <Example
    title='<CardHeader scale="display">'
    code={`
      <Card density="comfortable" tone="filled">
        <CardHeader titleAs="h4" scale="display" overline="Platform">
          <span>Ship the model, not the toolchain</span>
        </CardHeader>
        <CardContent>
          One card standing in for a hero, with nothing behind it but the type.
        </CardContent>
      </Card>
    `}
  >
    <Card density="comfortable" tone="filled">
      <CardHeader titleAs="h4" scale="display" overline="Platform">
        <span>Ship the model, not the toolchain</span>
      </CardHeader>
      <CardContent>
        One card standing in for a hero, with nothing behind it but the type.
      </CardContent>
    </Card>
  </Example>
</div>

### Compact

The same six with `density="compact"`. The mark, the list gap and the figure
row all come off the density hook, so nothing here is restyled per size.

<div
  className="gallery-grid gallery-grid--wide not-content"
  data-content-first="compact"
>
  <Example
    title='density="compact"'
    code={`
      <Card density="compact">
        <CardHeader titleAs="h4" overline="Profiling">
          <Icon slot="icon" name="gauge-high" />
          <span>Latency you can budget</span>
        </CardHeader>
        <CardContent>Every kernel reports its own cycle count.</CardContent>
      </Card>
    `}
  >
    <Card density="compact">
      <CardHeader titleAs="h4" overline="Profiling">
        <Icon slot="icon" name="gauge-high" />
        <span>Latency you can budget</span>
      </CardHeader>
      <CardContent>Every kernel reports its own cycle count.</CardContent>
    </Card>
  </Example>
  <Example
    title='<CardList marker="bullet" />'
    code={`
      <Card density="compact">
        <CardHeader titleAs="h4" overline="Included">
          <span>What the SDK ships with</span>
        </CardHeader>
        <CardList
          marker="bullet"
          items={[
            'Training recipes',
            'A device profiler',
            'Reference board builds',
          ]}
        />
      </Card>
    `}
  >
    <Card density="compact">
      <CardHeader titleAs="h4" overline="Included">
        <span>What the SDK ships with</span>
      </CardHeader>
      <CardList
        marker="bullet"
        items={[
          'Training recipes',
          'A device profiler',
          'Reference board builds',
        ]}
      />
    </Card>
  </Example>
  <Example
    title='step={2} density="compact"'
    code={`
      <Card density="compact">
        <CardHeader titleAs="h4" step={2} overline="Step two">
          <span>Quantise the graph</span>
        </CardHeader>
        <CardContent>Calibration travels with the artefact.</CardContent>
      </Card>
    `}
  >
    <Card density="compact">
      <CardHeader titleAs="h4" step={2} overline="Step two">
        <span>Quantise the graph</span>
      </CardHeader>
      <CardContent>Calibration travels with the artefact.</CardContent>
    </Card>
  </Example>
  <Example
    title="<CardQuote attribution initials>"
    code={`
      <Card density="compact">
        <CardQuote attribution="Dana Mercer" initials="DM">
          A week we did not spend guessing.
        </CardQuote>
      </Card>
    `}
  >
    <Card density="compact">
      <CardQuote attribution="Dana Mercer" initials="DM">
        A week we did not spend guessing.
      </CardQuote>
    </Card>
  </Example>
  <Example
    title='variant="stats" density="compact"'
    code={`
      <Card density="compact">
        <CardHeader titleAs="h4" overline="Measured on device">
          <span>Keyword spotting</span>
        </CardHeader>
        <CardContent variant="stats">
          <StatCard label="Latency">18 ms</StatCard>
          <StatCard label="Energy">2.4 mJ</StatCard>
        </CardContent>
      </Card>
    `}
  >
    <Card density="compact">
      <CardHeader titleAs="h4" overline="Measured on device">
        <span>Keyword spotting</span>
      </CardHeader>
      <CardContent variant="stats">
        <StatCard label="Latency">18 ms</StatCard>
        <StatCard label="Energy">2.4 mJ</StatCard>
      </CardContent>
    </Card>
  </Example>
  <Example
    title='scale="display" density="compact"'
    code={`
      <Card density="compact" tone="filled">
        <CardHeader titleAs="h4" scale="display" overline="Platform">
          <span>Ship the model, not the toolchain</span>
        </CardHeader>
        <CardContent>The type carries it at any density.</CardContent>
      </Card>
    `}
  >
    <Card density="compact" tone="filled">
      <CardHeader titleAs="h4" scale="display" overline="Platform">
        <span>Ship the model, not the toolchain</span>
      </CardHeader>
      <CardContent>The type carries it at any density.</CardContent>
    </Card>
  </Example>
</div>

## Named transitions

Five transitions, each built from the motion tokens and nothing else, so the
per-site dial and the reduced-motion rule reach all of them at once. Point at
the cards below; none of them does anything at rest.

### Hover targets

<div className="gallery-grid gallery-grid--wide not-content">
  <Example
    title=".helia-motion-surface (via interactive)"
    code={`
      <Card interactive>
        <CardHeader titleAs="h4">
          <span>Hover me: the fill shifts</span>
        </CardHeader>
        <CardContent>
          Background and border, 120ms. No transform: a card that moves under
          the pointer inside a grid reflows the row it shares.
        </CardContent>
      </Card>
    `}
  >
    <Card interactive>
      <CardHeader titleAs="h4">
        <span>Hover me: the fill shifts</span>
      </CardHeader>
      <CardContent>
        Background and border, 120ms. No transform: a card that moves under the
        pointer inside a grid reflows the row it shares.
      </CardContent>
    </Card>
  </Example>
  <Example
    title="<Card lift> / .helia-motion-lift"
    code={`
      <Card lift tone="elevated">
        <CardHeader titleAs="h4">
          <span>Hover me: the card lifts</span>
        </CardHeader>
        <CardContent>
          2px up and one shadow step, 200ms. Never a scale, which would
          resample the media and the text.
        </CardContent>
      </Card>
    `}
  >
    <Card lift tone="elevated">
      <CardHeader titleAs="h4">
        <span>Hover me: the card lifts</span>
      </CardHeader>
      <CardContent>
        2px up and one shadow step, 200ms. Never a scale, which would resample
        the media and the text.
      </CardContent>
    </Card>
  </Example>
  <Example
    title=".helia-motion-cue"
    code={`
      <LinkCard
        titleAs="h4"
        href="/helia-ui/gallery/#named-transitions"
        title="Hover me: the arrow nudges"
      >
        2px along, 120ms. The cue moves and the card holds still.
      </LinkCard>
    `}
  >
    <LinkCard
      titleAs="h4"
      href="/helia-ui/gallery/#named-transitions"
      title="Hover me: the arrow nudges"
    >
      2px along, 120ms. The cue moves and the card holds still.
    </LinkCard>
  </Example>
  <Example
    title=".helia-button"
    code={`
      <Card>
        <CardActions>
          <Button href="/helia-ui/gallery/#named-transitions" size="sm">
            <span>Hover me: the button fills</span>
          </Button>
          <Chip href="/helia-ui/gallery/#named-transitions">And the pill</Chip>
        </CardActions>
        <CardContent>
          Button and Chip are on the same 120ms step as the surface, so a card
          and the controls inside it settle together.
        </CardContent>
      </Card>
    `}
  >
    <Card>
      <CardActions>
        <Button href="/helia-ui/gallery/#named-transitions" size="sm">
          <span>Hover me: the button fills</span>
        </Button>
        <Chip href="/helia-ui/gallery/#named-transitions">And the pill</Chip>
      </CardActions>
      <CardContent>
        Button and Chip are on the same 120ms step as the surface, so a card and
        the controls inside it settle together.
      </CardContent>
    </Card>
  </Example>
</div>

### The carousel glide

The fifth transition. The rail reads the same dial the CSS does, so a site with
motion switched off gets the jump rather than the slide.

<div className="gallery-grid gallery-grid--full not-content">
  <Example
    title='<ShowcaseCarousel variant="compact" tabs={[...]}>'
    code={`
      <ShowcaseCarousel
        variant="compact"
        tabs={['Compile', 'Profile', 'Deploy', 'Measure']}
      >
        <Card>
          <CardMedia ratio="16/9">
            <Media
              treatment="color-field"
              style="--accent: var(--helia-accent-purple)"
            />
          </CardMedia>
          <CardHeader titleAs="h4">
            <span>Compile</span>
          </CardHeader>
        </Card>
        <Card>
          <CardMedia ratio="16/9">
            <Media
              treatment="color-field"
              style="--accent: var(--helia-accent-cyan)"
            />
          </CardMedia>
          <CardHeader titleAs="h4">
            <span>Profile</span>
          </CardHeader>
        </Card>
        <Card>
          <CardMedia ratio="16/9">
            <Media
              treatment="color-field"
              style="--accent: var(--helia-accent-green)"
            />
          </CardMedia>
          <CardHeader titleAs="h4">
            <span>Deploy</span>
          </CardHeader>
        </Card>
        <Card>
          <CardMedia ratio="16/9">
            <Media
              treatment="color-field"
              style="--accent: var(--helia-accent-orange)"
            />
          </CardMedia>
          <CardHeader titleAs="h4">
            <span>Measure</span>
          </CardHeader>
        </Card>
      </ShowcaseCarousel>
    `}
  >
    <ShowcaseCarousel
      variant="compact"
      tabs={['Compile', 'Profile', 'Deploy', 'Measure']}
    >
      <Card>
        <CardMedia ratio="16/9">
          <Media
            treatment="color-field"
            style="--accent: var(--helia-accent-purple)"
          />
        </CardMedia>
        <CardHeader titleAs="h4">
          <span>Compile</span>
        </CardHeader>
      </Card>
      <Card>
        <CardMedia ratio="16/9">
          <Media
            treatment="color-field"
            style="--accent: var(--helia-accent-cyan)"
          />
        </CardMedia>
        <CardHeader titleAs="h4">
          <span>Profile</span>
        </CardHeader>
      </Card>
      <Card>
        <CardMedia ratio="16/9">
          <Media
            treatment="color-field"
            style="--accent: var(--helia-accent-green)"
          />
        </CardMedia>
        <CardHeader titleAs="h4">
          <span>Deploy</span>
        </CardHeader>
      </Card>
      <Card>
        <CardMedia ratio="16/9">
          <Media
            treatment="color-field"
            style="--accent: var(--helia-accent-orange)"
          />
        </CardMedia>
        <CardHeader titleAs="h4">
          <span>Measure</span>
        </CardHeader>
      </Card>
    </ShowcaseCarousel>
  </Example>
</div>

## Theme knobs

The site theme is nine dials in one file. `:root` is where a site sets them;
a section that wants its own treatment sets them on a wrapper that carries
`data-helia-theme`, which is the hook `semantic.css` re-derives the composed
tokens on. That is the mechanism every example below uses, and it is why they
can sit next to each other on one page. A wrapper without the attribute moves
only the dials themselves, not the radius steps and tinted surfaces built out
of them.

<div className="gallery-grid gallery-grid--wide not-content">
  <Example
    title="--helia-radius-scale: 0"
    code={`
      <div
        className="gallery-stack"
        data-helia-theme="knob-square"
        data-helia-density="compact"
        style="--helia-radius-scale: 0; --helia-surface-tint: var(--helia-accent-orange); --helia-surface-tint-amount: 6%"
      >
        <Card tone="elevated">
          <CardContent>Square, compact, tinted orange.</CardContent>
        </Card>
        <Card tone="filled">
          <CardActions>
            <Button href="/helia-ui/gallery/#theme-knobs" size="sm">
              <span>Start</span>
            </Button>
            <Badge tone="secondary">Paired</Badge>
          </CardActions>
        </Card>
      </div>
    `}
  >
    <div
      className="gallery-stack"
      data-helia-theme="knob-square"
      data-helia-density="compact"
      style="--helia-radius-scale: 0; --helia-surface-tint: var(--helia-accent-orange); --helia-surface-tint-amount: 6%"
    >
      <Card tone="elevated">
        <CardContent>Square, compact, tinted orange.</CardContent>
      </Card>
      <Card tone="filled">
        <CardActions>
          <Button href="/helia-ui/gallery/#theme-knobs" size="sm">
            <span>Start</span>
          </Button>
          <Badge tone="secondary">Paired</Badge>
        </CardActions>
      </Card>
    </div>
  </Example>
  <Example
    title="--helia-radius-scale: 1 (the default)"
    code={`
      <div
        className="gallery-stack"
        data-helia-theme="knob-default"
        style="--helia-radius-scale: 1"
      >
        <Card tone="elevated">
          <CardContent>The package defaults, for comparison.</CardContent>
        </Card>
        <Card tone="filled">
          <CardActions>
            <Button href="/helia-ui/gallery/#theme-knobs" size="sm">
              <span>Start</span>
            </Button>
            <Badge tone="secondary">Paired</Badge>
          </CardActions>
        </Card>
      </div>
    `}
  >
    <div
      className="gallery-stack"
      data-helia-theme="knob-default"
      style="--helia-radius-scale: 1"
    >
      <Card tone="elevated">
        <CardContent>The package defaults, for comparison.</CardContent>
      </Card>
      <Card tone="filled">
        <CardActions>
          <Button href="/helia-ui/gallery/#theme-knobs" size="sm">
            <span>Start</span>
          </Button>
          <Badge tone="secondary">Paired</Badge>
        </CardActions>
      </Card>
    </div>
  </Example>
  <Example
    title="--helia-radius-scale: 1.6"
    code={`
      <div
        className="gallery-stack"
        data-helia-theme="knob-round"
        data-helia-density="comfortable"
        style="--helia-radius-scale: 1.6; --helia-surface-tint: var(--helia-accent-cyan); --helia-surface-tint-amount: 9%"
      >
        <Card tone="elevated">
          <CardContent>Round, comfortable, tinted cyan.</CardContent>
        </Card>
        <Card tone="filled">
          <CardActions>
            <Button href="/helia-ui/gallery/#theme-knobs" size="sm">
              <span>Start</span>
            </Button>
            <Badge tone="secondary">Paired</Badge>
          </CardActions>
        </Card>
      </div>
    `}
  >
    <div
      className="gallery-stack"
      data-helia-theme="knob-round"
      data-helia-density="comfortable"
      style="--helia-radius-scale: 1.6; --helia-surface-tint: var(--helia-accent-cyan); --helia-surface-tint-amount: 9%"
    >
      <Card tone="elevated">
        <CardContent>Round, comfortable, tinted cyan.</CardContent>
      </Card>
      <Card tone="filled">
        <CardActions>
          <Button href="/helia-ui/gallery/#theme-knobs" size="sm">
            <span>Start</span>
          </Button>
          <Badge tone="secondary">Paired</Badge>
        </CardActions>
      </Card>
    </div>
  </Example>
</div>

The accent dial reaches a card through its artwork rather than its edge: no
card part carries an accent rule or brim, so a subtree that names an accent
shows it in the media, the badges and the type. The second accent is the one
that pairs with it, and a link card spends it on the cue under the pointer.

<div className="gallery-grid not-content">
  <Example
    title="--helia-product-accent: var(--helia-accent-orange)"
    code={`
      <div data-helia-theme="knob-orange" style="--helia-product-accent: var(--helia-accent-orange)">
        <Card>
          <CardMedia>
            <Media treatment="color-field" style="--accent: var(--helia-product-accent)" />
          </CardMedia>
          <CardContent>An orange site.</CardContent>
        </Card>
      </div>
    `}
  >
    <div
      data-helia-theme="knob-orange"
      style="--helia-product-accent: var(--helia-accent-orange)"
    >
      <Card>
        <CardMedia>
          <Media
            treatment="color-field"
            style="--accent: var(--helia-product-accent)"
          />
        </CardMedia>
        <CardContent>An orange site.</CardContent>
      </Card>
    </div>
  </Example>
  <Example
    title="--helia-product-accent: var(--helia-accent-purple)"
    code={`
      <div data-helia-theme="knob-purple" style="--helia-product-accent: var(--helia-accent-purple)">
        <Card>
          <CardMedia>
            <Media treatment="color-field" style="--accent: var(--helia-product-accent)" />
          </CardMedia>
          <CardContent>A purple site.</CardContent>
        </Card>
      </div>
    `}
  >
    <div
      data-helia-theme="knob-purple"
      style="--helia-product-accent: var(--helia-accent-purple)"
    >
      <Card>
        <CardMedia>
          <Media
            treatment="color-field"
            style="--accent: var(--helia-product-accent)"
          />
        </CardMedia>
        <CardContent>A purple site.</CardContent>
      </Card>
    </div>
  </Example>
  <Example
    title="--helia-motion-scale: 0"
    code={`
      <div data-helia-theme="knob-still" style="--helia-motion-scale: 0">
        <Card lift tone="elevated">
          <CardContent>Motion off: the lift arrives instantly.</CardContent>
        </Card>
      </div>
    `}
  >
    <div data-helia-theme="knob-still" style="--helia-motion-scale: 0">
      <Card lift tone="elevated">
        <CardContent>Motion off: the lift arrives instantly.</CardContent>
      </Card>
    </div>
  </Example>
</div>

## Reveal on scroll

`Reveal` fades and rises what it wraps the first time it reaches the viewport.
Use it on a section, not on every card in one: a page where each element
arrives separately reads as a page that is still loading. `stagger` is the one
exception, and it is still one wrapper.

The hidden state is a class the script adds, so the content is visible without
JavaScript, and the script returns before arming anything when the motion scale
is 0.

Replay re-arms every reveal on this page, so the entry can be watched without
scrolling away and back. It does nothing for a visitor who asked for reduced
motion, because nothing armed.

<RevealReplay />

<div className="gallery-spacer not-content">
  Scroll on. The cards below start hidden and arrive as they reach the viewport.
</div>

<Example
  title="<Reveal>"
  code={`
    <Reveal>
      <div className="gallery-grid not-content">
        <Card tone="elevated">
          <CardContent>One section, one reveal.</CardContent>
        </Card>
        <Card tone="elevated">
          <CardContent>The cards inside it arrive together.</CardContent>
        </Card>
        <Card tone="elevated">
          <CardContent>Which is the point of wrapping the section.</CardContent>
        </Card>
      </div>
    </Reveal>
  `}
>
  <Reveal>
    <div className="gallery-grid not-content">
      <Card tone="elevated">
        <CardContent>One section, one reveal.</CardContent>
      </Card>
      <Card tone="elevated">
        <CardContent>The cards inside it arrive together.</CardContent>
      </Card>
      <Card tone="elevated">
        <CardContent>Which is the point of wrapping the section.</CardContent>
      </Card>
    </div>
  </Reveal>
</Example>

<div className="gallery-spacer not-content">
  A gap, so the staggered grid below is still out of view when the group above
  arrives.
</div>

The cascade is on the direct children, so the grid is the `Reveal` rather than
a `div` inside it: one wrapper with a grid class on it, and the cards are the
children that arrive.

<Example
  title="<Reveal stagger>"
  code={`
    <Reveal stagger class="gallery-grid not-content">
      <Card tone="elevated">
        <CardContent>With stagger, the children arrive in order.</CardContent>
      </Card>
      <Card tone="elevated">
        <CardContent>Each one waits a fast step longer than the last.</CardContent>
      </Card>
      <Card tone="elevated">
        <CardContent>The wrapper itself does not move.</CardContent>
      </Card>
      <Card tone="elevated">
        <CardContent>The cascade stops at the sixth child.</CardContent>
      </Card>
    </Reveal>
  `}
>
  <Reveal stagger class="gallery-grid not-content">
    <Card tone="elevated">
      <CardContent>With stagger, the children arrive in order.</CardContent>
    </Card>
    <Card tone="elevated">
      <CardContent>
        Each one waits a fast step longer than the last.
      </CardContent>
    </Card>
    <Card tone="elevated">
      <CardContent>The wrapper itself does not move.</CardContent>
    </Card>
    <Card tone="elevated">
      <CardContent>The cascade stops at the sixth child.</CardContent>
    </Card>
  </Reveal>
</Example>

<div className="gallery-spacer not-content">
  A second gap, so the group below is still out of view when the first arrives.
</div>

<Example
  title="<Reveal threshold={0.4}>"
  code={`
    <Reveal threshold={0.4}>
      <div className="gallery-grid not-content">
        <Card tone="filled">
          <CardContent>
            A higher threshold waits until more of the section is in view.
          </CardContent>
        </Card>
        <Card tone="filled">
          <CardContent>
            Lower it for a section too tall to ever cross the mark.
          </CardContent>
        </Card>
      </div>
    </Reveal>
  `}
>
  <Reveal threshold={0.4}>
    <div className="gallery-grid not-content">
      <Card tone="filled">
        <CardContent>
          A higher threshold waits until more of the section is in view.
        </CardContent>
      </Card>
      <Card tone="filled">
        <CardContent>
          Lower it for a section too tall to ever cross the mark.
        </CardContent>
      </Card>
    </div>
  </Reveal>
</Example>

## Page layout

Four parts that arrange a page rather than fill one. None of them knows what it
is carrying: the ground, the cards and the artwork are all handed in, so the
same four cover a product page, a release note and a landing.

### Band

`Band` takes a section to the edges of the reading frame and keeps its content
on the readable measure. `tone` picks the ground and `density` the rhythm above
and below it. None of the four grounds draws an edge or an elevation: a section
that carried either would be a very wide card.

<Example
  title='<Band tone="muted">'
  code={`
    <Band tone="muted">
      <div slot="header">
        <Eyebrow>Ground</Eyebrow>
        <h4>A section with a ground of its own</h4>
      </div>
      <p>
        The paint runs to the edges of the frame and the words stay on the
        measure. Nothing is drawn around it.
      </p>
    </Band>
  `}
>
  <div className="gallery-bleed">
    <Band tone="muted">
      <div slot="header">
        <Eyebrow>Ground</Eyebrow>
        <h4>A section with a ground of its own</h4>
      </div>
      <p>
        The paint runs to the edges of the frame and the words stay on the
        measure. Nothing is drawn around it.
      </p>
    </Band>
  </div>
</Example>

### Card grid

`CardGrid` is the row of peers. A named count steps down at the two
breakpoints; `auto` fits whatever `min` allows, which is the answer for a set
whose length the page does not know. `stagger` makes the grid the reveal
itself, so the cards cascade in rather than the block arriving as one.

<Example
  title="<CardGrid columns={3} equalize stagger>"
  code={`
    <CardGrid columns={3} equalize stagger class="not-content">
      <Card tone="filled">
        <CardHeader titleAs="h4">
          <span>Columns</span>
        </CardHeader>
        <CardContent>Two, three, four, or as many as the minimum allows.</CardContent>
      </Card>
      <Card tone="filled">
        <CardHeader titleAs="h4">
          <span>Equalise</span>
        </CardHeader>
        <CardContent>Every row takes the height of the tallest card on the grid.</CardContent>
      </Card>
      <Card tone="filled">
        <CardHeader titleAs="h4">
          <span>Stagger</span>
        </CardHeader>
        <CardContent>One wrapper, one arrival, told across the row.</CardContent>
      </Card>
    </CardGrid>
  `}
>
  <CardGrid columns={3} equalize stagger class="not-content">
    <Card tone="filled">
      <CardHeader titleAs="h4">
        <span>Columns</span>
      </CardHeader>
      <CardContent>
        Two, three, four, or as many as the minimum allows.
      </CardContent>
    </Card>
    <Card tone="filled">
      <CardHeader titleAs="h4">
        <span>Equalise</span>
      </CardHeader>
      <CardContent>
        Every row takes the height of the tallest card on the grid.
      </CardContent>
    </Card>
    <Card tone="filled">
      <CardHeader titleAs="h4">
        <span>Stagger</span>
      </CardHeader>
      <CardContent>One wrapper, one arrival, told across the row.</CardContent>
    </Card>
  </CardGrid>
</Example>

### Masonry

`Masonry` packs cards of unequal height into columns with no dead space under
the short ones. It is CSS columns and no script, so the packing is right in the
first paint; the cost is a reading order that runs down each column before it
moves across.

<Example
  title="<Masonry columns={3}>"
  code={`
    <Masonry columns={3} class="not-content">
      <Card tone="filled">
        <CardQuote attribution="Dana Mercer" detail="Firmware lead" initials="DM">
          The profile told us where the cycles went on the first afternoon.
        </CardQuote>
      </Card>
      <Card tone="filled">
        <CardHeader titleAs="h4">
          <span>Uneven by design</span>
        </CardHeader>
        <CardContent>
          A short card next to a long one is what the layout is for. Forcing the
          two to one height is what makes a set of notes look padded.
        </CardContent>
      </Card>
      <Card tone="filled">
        <CardHeader titleAs="h4" overline="Included">
          <span>What the column keeps</span>
        </CardHeader>
        <CardList
          items={[
            'Each card whole across a break',
            'The gap on the spacing scale',
            'One column below 42rem',
          ]}
        />
      </Card>
    </Masonry>
  `}
>
  <Masonry columns={3} class="not-content">
    <Card tone="filled">
      <CardQuote attribution="Dana Mercer" detail="Firmware lead" initials="DM">
        The profile told us where the cycles went on the first afternoon.
      </CardQuote>
    </Card>
    <Card tone="filled">
      <CardHeader titleAs="h4">
        <span>Uneven by design</span>
      </CardHeader>
      <CardContent>
        A short card next to a long one is what the layout is for. Forcing the
        two to one height is what makes a set of notes look padded.
      </CardContent>
    </Card>
    <Card tone="filled">
      <CardHeader titleAs="h4" overline="Included">
        <span>What the column keeps</span>
      </CardHeader>
      <CardList
        items={[
          'Each card whole across a break',
          'The gap on the spacing scale',
          'One column below 42rem',
        ]}
      />
    </Card>
  </Masonry>
</Example>

### Mosaic

`Mosaic` gives one card the room to carry artwork and fills the rest of the set
around it. The feature is the first child rather than a prop, so the
arrangement follows reading order. `pattern` moves it to the end of the set or
across the top.

<Example
  title="<Mosaic>"
  code={`
    <Mosaic class="not-content">
      <Card tone="elevated" media="background">
        <CardMedia>
          <Media treatment="color-field" style="--accent: var(--helia-accent-cyan)" />
        </CardMedia>
        <CardHeader titleAs="h4">
          <span>The feature</span>
        </CardHeader>
        <CardContent>Two columns by two rows, with somewhere to put the artwork.</CardContent>
      </Card>
      <Card tone="filled">
        <CardContent>The rest fill the remaining cells.</CardContent>
      </Card>
      <Card tone="filled">
        <CardContent>In the order they were written.</CardContent>
      </Card>
      <Card tone="filled">
        <CardContent>Nothing here is placed by hand.</CardContent>
      </Card>
    </Mosaic>
  `}
>
  <Mosaic class="not-content">
    <Card tone="elevated" media="background">
      <CardMedia>
        <Media
          treatment="color-field"
          style="--accent: var(--helia-accent-cyan)"
        />
      </CardMedia>
      <CardHeader titleAs="h4">
        <span>The feature</span>
      </CardHeader>
      <CardContent>
        Two columns by two rows, with somewhere to put the artwork.
      </CardContent>
    </Card>
    <Card tone="filled">
      <CardContent>The rest fill the remaining cells.</CardContent>
    </Card>
    <Card tone="filled">
      <CardContent>In the order they were written.</CardContent>
    </Card>
    <Card tone="filled">
      <CardContent>Nothing here is placed by hand.</CardContent>
    </Card>
  </Mosaic>
</Example>

### The four together

The rest of this section is not an example on a stage: the bands are on the
page itself, which is the only way to see a ground actually run to the edges
and a run of sections read as one rhythm. Scroll it as a page rather than as a
set of parts.

<Band tone="plain" density="compact" data-page-layout-composition>
  <div slot="header">
    <Eyebrow>Overview</Eyebrow>
    <h4>One feature and the set around it</h4>
  </div>
  <Mosaic class="not-content">
    <Card tone="elevated" media="background">
      <CardMedia>
        <Media
          treatment="color-field"
          style="--accent: var(--helia-accent-purple)"
        />
      </CardMedia>
      <CardHeader titleAs="h4" overline="Featured">
        <span>The one the section is about</span>
      </CardHeader>
      <CardContent>
        Artwork needs room, and a grid of equals says nothing about which card
        is the point.
      </CardContent>
    </Card>
    <Card tone="filled">
      <CardHeader titleAs="h4">
        <span>Beside it</span>
      </CardHeader>
      <CardContent>Peers, at the size a peer deserves.</CardContent>
    </Card>
    <Card tone="filled">
      <CardHeader titleAs="h4">
        <span>Under it</span>
      </CardHeader>
      <CardContent>Filled in document order.</CardContent>
    </Card>
    <Card tone="filled">
      <CardHeader titleAs="h4">
        <span>And on</span>
      </CardHeader>
      <CardContent>The set is as long as the page needs.</CardContent>
    </Card>
  </Mosaic>
</Band>

<Band tone="muted">
  <div slot="header">
    <Eyebrow>Capabilities</Eyebrow>
    <h4>Three peers on a ground</h4>
  </div>
  <CardGrid columns={3} equalize class="not-content">
    <Card>
      <CardHeader titleAs="h4">
        <span>The ground changes</span>
      </CardHeader>
      <CardContent>
        The section above it has none, so this one reads as a step rather than
        as a box.
      </CardContent>
    </Card>
    <Card>
      <CardHeader titleAs="h4">
        <span>The measure does not</span>
      </CardHeader>
      <CardContent>
        Paint to the edges, text on the same width as the prose above it.
      </CardContent>
    </Card>
    <Card>
      <CardHeader titleAs="h4">
        <span>Nothing is drawn</span>
      </CardHeader>
      <CardContent>
        No edge, no brim, no shadow. The ground is the whole of the signal.
      </CardContent>
    </Card>
  </CardGrid>
</Band>

<Band tone="plain">
  <div slot="header">
    <Eyebrow>Notes</Eyebrow>
    <h4>What came back from the field</h4>
  </div>
  <Masonry columns={3} class="not-content">
    <Card tone="filled">
      <CardQuote attribution="Priya Raman" detail="Platform" initials="PR">
        We stopped arguing about section spacing the week the bands landed.
      </CardQuote>
    </Card>
    <Card tone="filled">
      <CardHeader titleAs="h4">
        <span>Content first</span>
      </CardHeader>
      <CardContent>
        Good artwork is the scarcest thing a card family depends on, so the
        options that carry a card without an image are the ones a long page
        leans on.
      </CardContent>
    </Card>
    <Card tone="filled">
      <CardHeader titleAs="h4" overline="Covered">
        <span>What a note can hold</span>
      </CardHeader>
      <CardList
        items={['A quote and who said it', 'A short list', 'A line of prose']}
      />
    </Card>
    <Card tone="filled">
      <CardQuote attribution="Sam Okafor" initials="SO">
        Three columns, no measuring, no layout shift when the font lands.
      </CardQuote>
    </Card>
  </Masonry>
</Band>

<Band tone="contrast" density="compact">
  <div slot="header">
    <h4>The close</h4>
  </div>
  <p>
    The inked ground is pinned dark in both themes, because ink that follows the
    theme over a ground that does not is a section that reads half the time.
  </p>
</Band>

## Charts

`Chart` draws with Observable Plot at build and ships the result as inline SVG,
so a page with five charts on it still ships no chart JavaScript. The palette is
written as `var(--helia-chart-*)`, which is what lets a figure that will never be
redrawn follow the theme toggle.

The title and the subtitle are the part's HTML rather than Plot's type, and the
subtitle says what is plotted against what — which is why no axis is titled.
Gridlines run across the measure; only a scatter, which has a measure on both
axes, gets the second set.

Every number below is invented and describes this documentation site. Nothing
here is a measurement of any part.

<Example
  title='kind="line" with two series'
  code={`
    <Chart
      title="Weekly page views"
      subtitle="Page views against week of the quarter, by section"
      caption="Synthetic series."
      kind="line"
      data={trafficSeries}
      x="week"
      y="views"
      series="section"
      width={900}
      height={280}
    />
  `}
>
  <Chart
    title="Weekly page views"
    subtitle="Page views against week of the quarter, by section"
    caption="Synthetic series."
    kind="line"
    data={trafficSeries}
    x="week"
    y="views"
    series="section"
    width={900}
    height={280}
  />
</Example>

<Example
  title='kind="bar" grouped by series'
  code={`
    <Chart
      title="Build time by stage"
      subtitle="Wall-clock seconds against build stage, by cache profile"
      kind="bar"
      data={buildSeries}
      x="stage"
      y="seconds"
      series="profile"
      width={900}
      height={280}
    />
  `}
>
  <Chart
    title="Build time by stage"
    subtitle="Wall-clock seconds against build stage, by cache profile"
    kind="bar"
    data={buildSeries}
    x="stage"
    y="seconds"
    series="profile"
    width={900}
    height={280}
  />
</Example>

<Example
  title='kind="area", one series'
  code={`
    <Chart
      title="Pages published"
      subtitle="Pages on the site against week of the quarter"
      kind="area"
      data={pagesSeries}
      x="week"
      y="pages"
      width={900}
      height={260}
    />
  `}
>
  <Chart
    title="Pages published"
    subtitle="Pages on the site against week of the quarter"
    kind="area"
    data={pagesSeries}
    x="week"
    y="pages"
    width={900}
    height={260}
  />
</Example>

<Example
  title='kind="scatter", gridlines on both axes'
  code={`
    <Chart
      title="Weight against score"
      subtitle="Performance score against transferred kilobytes, by template"
      kind="scatter"
      data={weightSeries}
      x="transferKb"
      y="score"
      series="template"
      width={900}
      height={280}
    />
  `}
>
  <Chart
    title="Weight against score"
    subtitle="Performance score against transferred kilobytes, by template"
    kind="scatter"
    data={weightSeries}
    x="transferKb"
    y="score"
    series="template"
    width={900}
    height={280}
  />
</Example>

### Three across

`ChartGroup` gives the charts equal columns, so three drawn at the same `width`
and `height` line up across the row and read as one figure. Draw them at a width
near the column, not at the page width: the SVG scales into its column and takes
its type with it.

<Example
  title="<ChartGroup> of three"
  code={`
    <ChartGroup>
      <Chart
        title="Page views"
        subtitle="Views against week, by section"
        kind="line"
        data={trafficSeries}
        x="week"
        y="views"
        series="section"
        width={300}
        height={200}
        density="compact"
      />
      <Chart
        title="Build time"
        subtitle="Seconds against stage, by cache profile"
        kind="bar"
        data={buildSeries}
        x="stage"
        y="seconds"
        series="profile"
        width={300}
        height={200}
        density="compact"
      />
      <Chart
        title="Pages published"
        subtitle="Pages against week"
        kind="area"
        data={pagesSeries}
        x="week"
        y="pages"
        width={300}
        height={200}
        density="compact"
      />
    </ChartGroup>
  `}
>
  <ChartGroup>
    <Chart
      title="Page views"
      subtitle="Views against week, by section"
      kind="line"
      data={trafficSeries}
      x="week"
      y="views"
      series="section"
      width={300}
      height={200}
      density="compact"
    />
    <Chart
      title="Build time"
      subtitle="Seconds against stage, by cache profile"
      kind="bar"
      data={buildSeries}
      x="stage"
      y="seconds"
      series="profile"
      width={300}
      height={200}
      density="compact"
    />
    <Chart
      title="Pages published"
      subtitle="Pages against week"
      kind="area"
      data={pagesSeries}
      x="week"
      y="pages"
      width={300}
      height={200}
      density="compact"
    />
  </ChartGroup>
</Example>

## Other parts

The rest of the Astro parts, one example each. They are documented in depth on
their own pages; they are here so that one page answers what the package can
draw.

<Example
  title="<CodeTabs tabs={[...]} />"
  code={`
    <CodeTabs
      ariaLabel="Average latency implementation"
      tabs={[
        {
          label: 'TypeScript',
          language: 'typescript',
          code: 'const average = total / samples.length;',
        },
        {
          label: 'Python',
          language: 'python',
          code: 'average = total / len(samples)',
        },
      ]}
    />
  `}
>
  <CodeTabs
    ariaLabel="Average latency implementation"
    tabs={[
      {
        label: 'TypeScript',
        language: 'typescript',
        code: 'const average = total / samples.length;',
      },
      {
        label: 'Python',
        language: 'python',
        code: 'average = total / len(samples)',
      },
    ]}
  />
</Example>

<Example
  title="<AsciiTerminal lines={[...]} />"
  code={`
    <AsciiTerminal
      title="Expected output"
      lines={[
        { kind: 'command', text: 'npm run build' },
        { kind: 'muted', text: 'Resolving configuration…' },
        { kind: 'output', text: '  ENTRY     src/index.ts' },
        { kind: 'output', text: '  TARGET    es2022' },
        { kind: 'warning', text: '  ! no accent colour set' },
        { kind: 'error', text: '  x missing tsconfig path alias' },
        { kind: 'success', text: '✓ Build complete' },
      ]}
    />
  `}
>
  <AsciiTerminal
    title="Expected output"
    lines={[
      { kind: 'command', text: 'npm run build' },
      { kind: 'muted', text: 'Resolving configuration…' },
      { kind: 'output', text: '  ENTRY     src/index.ts' },
      { kind: 'output', text: '  TARGET    es2022' },
      { kind: 'warning', text: '  ! no accent colour set' },
      { kind: 'error', text: '  x missing tsconfig path alias' },
      { kind: 'success', text: '✓ Build complete' },
    ]}
  />
</Example>

<Example
  title="<Callout title tone>"
  code={`
    <div className="helia-callout-stack">
      <Callout title="Keep the calibration set">
        The converter writes it beside the artefact, so the next run starts from
        the same numbers.
      </Callout>
      <Callout tone="warning" title="Density is inherited">
        A part that names its own step opts out of the hook and keeps it.
      </Callout>
    </div>
  `}
>
  <div className="helia-callout-stack">
    <Callout title="Keep the calibration set">
      The converter writes it beside the artefact, so the next run starts from
      the same numbers.
    </Callout>
    <Callout tone="warning" title="Density is inherited">
      A part that names its own step opts out of the hook and keeps it.
    </Callout>
  </div>
</Example>

<Example
  title="<Timeline groups={[...]} />"
  code={`
    <Timeline
      heading="What's new"
      groups={[
        {
          label: 'Week two',
          items: [
            {
              title: 'The gallery shows its own source',
              description:
                'Every example carries the MDX that renders it.',
            },
          ],
        },
        {
          label: 'Week one',
          items: [{ title: 'Content-first cards landed' }],
        },
      ]}
    />
  `}
>
  <Timeline
    heading="What's new"
    groups={[
      {
        label: 'Week two',
        items: [
          {
            title: 'The gallery shows its own source',
            description: 'Every example carries the MDX that renders it.',
          },
        ],
      },
      {
        label: 'Week one',
        items: [{ title: 'Content-first cards landed' }],
      },
    ]}
  />
</Example>

<Example
  title="<AccordionGroup items={[...]} />"
  code={`
    <AccordionGroup
      items={[
        {
          title: 'When does a variant earn a place here?',
          content:
            'When it is a modifier on a part rather than a part of its own.',
        },
        {
          title: 'Where are the reference tables?',
          content: 'On the page for the part, not on this one.',
        },
      ]}
    />
  `}
>
  <AccordionGroup
    items={[
      {
        title: 'When does a variant earn a place here?',
        content:
          'When it is a modifier on a part rather than a part of its own.',
      },
      {
        title: 'Where are the reference tables?',
        content: 'On the page for the part, not on this one.',
      },
    ]}
  />
</Example>

<Example
  title='<EditorialBand tone="paper">'
  code={`
    <EditorialBand tone="paper">
      <SectionHeader
        eyebrow="Chapter"
        title="A band, not a card"
        description="No border, no radius, no shadow: a chapter-level section."
      />
    </EditorialBand>
  `}
>
  <EditorialBand tone="paper">
    <SectionHeader
      eyebrow="Chapter"
      title="A band, not a card"
      description="No border, no radius, no shadow: a chapter-level section."
    />
  </EditorialBand>
</Example>

<Example
  title="<SectionHeader eyebrow title description href>"
  code={`
    <SectionHeader
      eyebrow="Parts"
      title="Everything the package draws"
      description="One row above a section, with the link that leaves it."
      href="/helia-ui/reference/astro-parts/"
      linkLabel="All contracts"
    />
  `}
>
  <SectionHeader
    eyebrow="Parts"
    title="Everything the package draws"
    description="One row above a section, with the link that leaves it."
    href="/helia-ui/reference/astro-parts/"
    linkLabel="All contracts"
  />
</Example>

<Example
  title="<DataTable caption columns rows />"
  code={`
    <DataTable
      caption="Card tones"
      columns={['Tone', 'What stands in for the edge']}
      rows={[
        ['elevated', 'A shadow'],
        ['outlined', 'A hairline border'],
        ['filled', 'A muted fill'],
        ['ghost', 'Nothing, until the pointer arrives'],
      ]}
    />
  `}
>
  <DataTable
    caption="Card tones"
    columns={['Tone', 'What stands in for the edge']}
    rows={[
      ['elevated', 'A shadow'],
      ['outlined', 'A hairline border'],
      ['filled', 'A muted fill'],
      ['ghost', 'Nothing, until the pointer arrives'],
    ]}
  />
</Example>

<Example
  title="<MediaEmbed title videoId />"
  code={`
    <MediaEmbed
      title="YouTube embed sample"
      videoId="M7lc1UVf-VE"
      caption="The player is not requested until the visitor presses play."
      duration="1:24"
    />
  `}
>
  <MediaEmbed
    title="YouTube embed sample"
    videoId="M7lc1UVf-VE"
    caption="The player is not requested until the visitor presses play."
    duration="1:24"
  />
</Example>

<Example
  title="<Eyebrow>"
  code={`
    <Eyebrow>Section label</Eyebrow>
    <Eyebrow as="span">Inline label</Eyebrow>
  `}
>
  <Eyebrow>Section label</Eyebrow>
  <Eyebrow as="span">Inline label</Eyebrow>
</Example>

<Example
  title="<Chip tone size href>"
  code={`
    <div className="helia-badge-row">
      <Chip>Neutral</Chip>
      <Chip tone="accent">Accent</Chip>
      <Chip tone="static">Static</Chip>
      <Chip href="/helia-ui/gallery/#other-parts">Linked</Chip>
      <Chip size="md">Medium</Chip>
      <Chip size="md" tone="static">
        Medium static
      </Chip>
    </div>
  `}
>
  <div className="helia-badge-row">
    <Chip>Neutral</Chip>
    <Chip tone="accent">Accent</Chip>
    <Chip tone="static">Static</Chip>
    <Chip href="/helia-ui/gallery/#other-parts">Linked</Chip>
    <Chip size="md">Medium</Chip>
    <Chip size="md" tone="static">
      Medium static
    </Chip>
  </div>
</Example>

<Example
  title="<Button variant size>"
  code={`
    <div className="helia-display-row">
      <Button href="/helia-ui/gallery/#other-parts">Primary</Button>
      <Button href="/helia-ui/gallery/#other-parts" variant="secondary">
        Secondary
      </Button>
      <Button href="/helia-ui/gallery/#other-parts" variant="ghost">
        Ghost
      </Button>
      <Button href="/helia-ui/gallery/#other-parts" size="sm">
        Primary, small
      </Button>
      <Button
        href="/helia-ui/gallery/#other-parts"
        variant="secondary"
        size="sm"
      >
        Secondary, small
      </Button>
      <Button href="/helia-ui/gallery/#other-parts" variant="ghost" size="sm">
        Ghost, small
      </Button>
    </div>
  `}
>
  <div className="helia-display-row">
    <Button href="/helia-ui/gallery/#other-parts">Primary</Button>
    <Button href="/helia-ui/gallery/#other-parts" variant="secondary">
      Secondary
    </Button>
    <Button href="/helia-ui/gallery/#other-parts" variant="ghost">
      Ghost
    </Button>
    <Button href="/helia-ui/gallery/#other-parts" size="sm">
      Primary, small
    </Button>
    <Button href="/helia-ui/gallery/#other-parts" variant="secondary" size="sm">
      Secondary, small
    </Button>
    <Button href="/helia-ui/gallery/#other-parts" variant="ghost" size="sm">
      Ghost, small
    </Button>
  </div>
</Example>

<Example
  title="<Badge tone size dot>"
  code={`
    <div className="helia-badge-row">
      <Badge>Neutral</Badge>
      <Badge tone="info">Info</Badge>
      <Badge tone="success">Success</Badge>
      <Badge tone="warning">Warning</Badge>
      <Badge tone="danger">Danger</Badge>
      <Badge tone="accent">Accent</Badge>
      <Badge size="md">Neutral, medium</Badge>
      <Badge size="md" tone="info">
        Info, medium
      </Badge>
      <Badge size="md" tone="success" dot>
        With a dot
      </Badge>
    </div>
  `}
>
  <div className="helia-badge-row">
    <Badge>Neutral</Badge>
    <Badge tone="info">Info</Badge>
    <Badge tone="success">Success</Badge>
    <Badge tone="warning">Warning</Badge>
    <Badge tone="danger">Danger</Badge>
    <Badge tone="accent">Accent</Badge>
    <Badge size="md">Neutral, medium</Badge>
    <Badge size="md" tone="info">
      Info, medium
    </Badge>
    <Badge size="md" tone="success" dot>
      With a dot
    </Badge>
  </div>
</Example>

<Example
  title="<Icon name>"
  code={`
    <div className="helia-display-row">
      <span>
        <Icon name="gauge-high" /> gauge-high
      </span>
      <span>
        <Icon name="memory" /> memory
      </span>
      <span>
        <Icon name="microchip" /> microchip
      </span>
      <span>
        <Icon name="chart-line" /> chart-line
      </span>
      <span>
        <Icon name="circle-info" /> circle-info
      </span>
      <span>
        <Icon name="arrow-right" /> arrow-right
      </span>
    </div>
  `}
>
  <div className="helia-display-row">
    <span>
      <Icon name="gauge-high" /> gauge-high
    </span>
    <span>
      <Icon name="memory" /> memory
    </span>
    <span>
      <Icon name="microchip" /> microchip
    </span>
    <span>
      <Icon name="chart-line" /> chart-line
    </span>
    <span>
      <Icon name="circle-info" /> circle-info
    </span>
    <span>
      <Icon name="arrow-right" /> arrow-right
    </span>
  </div>
</Example>
