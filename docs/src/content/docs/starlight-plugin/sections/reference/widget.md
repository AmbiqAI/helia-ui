---
title: Widget API
description: A generated-shaped reference page in the fixture's Demo reference section.
---

Demo reference is an `autogenerate` directory rather than a list, which is the
shape a generated reference lands in. The entries sit at the top of the pane,
one level, because the section itself is the group they would otherwise be
nested under.
