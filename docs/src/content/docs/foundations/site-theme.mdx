---
title: Site theme
description: The dials a HELIA site may set for itself, what each one moves, and what reads it.
---

{/* Generated by scripts/site-theme-doc.mjs. Edit site-theme.css, not this file. */}

import SiteThemeExamples from '../../../components/SiteThemeExamples.astro';

Flair lives in these dials and nowhere else. A site copies
`@ambiqai/helia-ui/site-theme.css` to `src/styles/site-theme.css`, loads it
from `customCss` immediately after the package sheets, and sets the values
below; everything else it might want to look different about is a package
change, not a site one. A site that needs a rule outside this contract opens
a component request against
[AmbiqAI/helia-ui](https://github.com/AmbiqAI/helia-ui/issues/new), so that
every consumer gets the same answer rather than ten sites each solving it in
their own stylesheet.

## The dials

Read out of `site-theme.css` itself: the declaration is the default and the
comment above it is the description, so a dial that moves without this page
moving fails `validate`.

| Dial                                                      | What it changes                                                                                                                               | Accepts                                                                                                                   | Default                                                   | Read by                                                                                                                                                                                                                                                   |
| --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `--helia-product-accent`                                  | The site's accent. One value, not a vocabulary: a site carrying several product accents sets this per subtree rather than adding tokens.      | Any colour, normally one of the `--helia-accent-*` hues rather than a fresh one, so the palette stays the palette.        | `var(--helia-accent-slate)`                               | `AsciiTerminal`, for the prompt colour. No card recipe draws an accent edge, so elsewhere a site passes this into `Media` as `--accent`.                                                                                                                  |
| `--helia-accent-secondary`                                | The second accent, for a treatment that needs two: a gradient, a chart series, a hero band. Leave it equal to the first and nothing pairs.    | Any colour, on the same terms as the first accent.                                                                        | `var(--helia-accent-slate)`                               | Nothing in the package. It is named here so that the sites which do pair two colours all spell the second one the same way.                                                                                                                               |
| `--helia-radius-scale`                                    | Multiplies the whole radius scale at once.                                                                                                    | A unitless number. 0 is a square site, 1 is the package shape, above about 1.6 the pill and the card stop being distinct. | `1`                                                       | `--helia-radius-xs`, `-sm`, `-md` and `-lg` in `semantic.css`, so every `helia-surface` and every part that rounds its own chrome. `--helia-radius-pill` is deliberately outside the scale.                                                               |
| `--helia-motion-scale`                                    | Multiplies every motion duration. Reduced motion zeroes it regardless, so this is the site's dial, not the visitor's.                         | A unitless number. 0 removes motion for everyone on the site; 1.5 is the slowest worth asking for.                        | `1`                                                       | `--helia-motion-fast`, `-base` and `-slow`, so `helia-motion-lift`, `helia-motion-reveal`, `helia-motion-cue`, `helia-hover-surface` and `helia-button`. `Reveal` and `ShowcaseCarousel` also read the number itself and skip their scripts when it is 0. |
| `--helia-font-sans`                                       | Interface and editorial type.                                                                                                                 | A full font stack, not a family name on its own.                                                                          | `var(--__sl-font, var(--helia-sl-font-system))`           | The page frame through Starlight, and `CodeBlock` for its chrome.                                                                                                                                                                                         |
| `--helia-font-mono`                                       | Code, filenames, versions, transcripts.                                                                                                       | A full font stack, not a family name on its own.                                                                          | `var(--__sl-font-mono, var(--helia-sl-font-system-mono))` | `CodeBlock` and `AsciiTerminal`.                                                                                                                                                                                                                          |
| `--helia-surface-tint`<br />`--helia-surface-tint-amount` | A hue mixed into every surface, and how much of it. The default mixes nothing, which is why the amount rather than the hue is what is zeroed. | Any colour, and a percentage. Keep the amount in single figures: this is a cast over the page, not a background colour.   | `transparent`<br />`0%`                                   | `--helia-surface-paper`, `--helia-surface-card` and `--helia-surface-card-muted`, so `helia-surface` and its variants, `helia-chip--accent`, `helia-chip--static` and `helia-button--ghost`.                                                              |
| `--helia-hero-treatment`                                  | How the site's hero is drawn.                                                                                                                 | `plain`, `band`, or `artwork`.                                                                                            | `plain`                                                   | Nothing in the package. A site's own hero composition reads it, and the package defines the name so that every site spells the choice the same way.                                                                                                       |
| `data-helia-density`                                      | The card padding step, and nothing else. Prose rhythm and the page frame belong to Starlight.                                                 | `compact`, `comfortable`, or the attribute left off.                                                                      | `unset`                                                   | `--helia-card-pad`, so `helia-surface--pad-card`.                                                                                                                                                                                                         |

Every default is the package default restated, so a copy with nothing changed
renders exactly what the package renders, and a site owns its flair as a diff
against one file.

## Two sites, one system

The same three cards and the same section header, under the hub’s theme and
under a warmer one. Nothing below is a component variant: the difference is
the values in a `site-theme.css`, set here on a wrapper so both can sit on one
page.

<SiteThemeExamples />

The second theme lives in this site as a scoped example rather than in the
package. A second theme is a thing a site has.

One caveat that the side-by-side forces into the open: these are `:root`
dials. `semantic.css` composes the radius steps and the tinted surfaces out
of them once, on the element that declares them, so a dial set further down
the tree never reaches a token composed further up. The examples above
restate those two compositions inside their own scope to get around it. A
site never needs to, because a site sets the dials where the package expects
them.
